"use client"
import { Metadata } from "next"

const metadata: Metadata = {
    title: "login",
    description: "login page",
}
export default function Page() {
    return (
        <main>
            <h1>Login Page</h1>

        </main>
    )
};